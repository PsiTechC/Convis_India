# Legacy call_handlers package. All voice media is now routed through LiveKit
# (see app/services/livekit/). This package is kept as a stub so any stale
# imports fail fast instead of masking a broken migration.
